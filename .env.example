# LLM — GLM-5.2 via z.ai OpenAI-compatible endpoint (your existing brain, zero new cost)
# Get key from https://z.ai/  (same GLM_API_KEY you already use)
GLM_API_KEY=
GLM_MODEL=glm-5.2
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4/

# Optional: Band platform endpoint overrides (defaults to production)
# BAND_WS_URL=
# BAND_REST_URL=

# Fallback: swap to Claude by setting these instead and changing _common.py model factory
# ANTHROPIC_API_KEY=
